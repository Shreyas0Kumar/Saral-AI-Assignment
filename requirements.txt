# Served image. Deliberately contains no torch, no transformers, no
# sentence-transformers: the hot path is a lexicon plus a sparse matrix
# multiply, and shipping an 800MB deep-learning stack to run neither would be
# paying for a capability the service does not use.
pydantic>=2.5,<3
pyyaml>=6.0
fastapi>=0.110
uvicorn[standard]>=0.27
numpy>=1.24,<3
scipy>=1.10
# Pinned to the minor that produced config/distilled_lr.joblib. An
# unpinned sklearn unpickles the artifact with an
# InconsistentVersionWarning and 'might lead to invalid results' --
# not something to discover from a ranking that quietly got worse.
# Regenerate the artifact (make regenerate-llm-artifacts) before
# widening this.
scikit-learn>=1.6,<1.7
joblib>=1.3
psutil>=5.9
