# Interview-defence notes, not part of the submission.
DECISIONS.md
build_plan.md
brief.txt

# Local editor / tool state.
.claude/settings.local.json
.idea/
.vscode/

__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
build/
dist/

# Runtime state. Rebuilt by `make all`; the committed artefacts live in config/.
*.db
*.sqlite3
# out/run_manifest.json IS committed: INFRA.md cites it as the source for every
# throughput and cost figure, and a claim whose evidence is gitignored is not
# a claim a reviewer can check.
.coverage
bash.exe.stackdump

# Cached LLM responses from the offline cost-arm script. Keyed by content hash,
# opaque, and regenerable; the measured summaries in out/llm_cost_arm.json are
# the evidence worth committing.
out/llm_cache/
